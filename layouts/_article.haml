%div.content-container
  %div.content.article
    %p.index-link-container
      %a.index-link{:href => "/"} ← All posts
    
    %h1{:property => "rss:title", :datatype => ""}
      = title_of(item)
    
    %p.date{:property => "dc:date", :content => item[:timestamp].to_s, :datatype => "xs:date"}
      = item[:timestamp].strftime("%A ") + item[:timestamp].day.to_s
      %sup<>= item[:timestamp].day.ordinal_suffix
      = item[:timestamp].strftime(" %B, %Y")
    
    %div.contents= find_and_preserve(content)
